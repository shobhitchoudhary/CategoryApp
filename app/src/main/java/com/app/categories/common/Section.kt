package com.app.categories.common

enum class Section(val sectionType: String) {
    BANNER(sectionType = "banner"),
    HORIZONTAL_SCROLL(sectionType = "horizontalFreeScroll"),
    SPLIT_BANNER(sectionType = "splitBanner")
}